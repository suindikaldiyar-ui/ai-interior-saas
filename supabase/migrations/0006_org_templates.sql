-- Свои типовые решения компании.
--
-- Шесть встроенных шаблонов закрывают большинство заказов, но у каждой
-- компании есть свои: «наша базовая на 2700 без посудомойки». Заводятся
-- через интерфейс и не требуют ни строчки кода — как и категории каталога.

alter table orgs
  add column if not exists run_templates jsonb not null default '[]'::jsonb;
